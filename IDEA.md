Taste recommandation :
AI agentic workflow, the AI agent ask llm generated question to the user, then recommand wine : it then creates a user profile based on the cellar + questions, : Important it updates the profile itself on each tasting,

Why it is agentic : Calls a first agent to generate a list of attributes to fill : agent returns the list of attributes it needs. It calls an llm to generate questions, user answers, the orchestrator receives the response, now an llm parses the answer for key vocalubary : both updates the vocabulary of the user, and updates the wine taste of the user.
